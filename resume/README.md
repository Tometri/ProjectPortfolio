# Resume

Resume documents and versions are stored here.

## Suggested Organization

1. Keep one current resume in `current/`.
2. Move older versions into `archive/`.
3. Use consistent file naming: `lastname-firstname-resume-YYYY-MM`.
